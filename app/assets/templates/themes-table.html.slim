.row
  .col-md-6
    table.table
      thead
        tr
          th Tema
          th &nbsp;
      tbody
        tr ng-repeat="theme in subject.themes"
          td
            | {{theme.title}}
          td
            a ng-click="removeTheme(theme.id)" style="cursor: pointer"
              span.glyphicon.glyphicon-remove aria-hidden="true"
